from setuptools import setup

setup(
  name='eml_parser',
  version = '0.5',
  description='Python EML parser library',
  author='Georges Toth',
  author_email='georges@trypill.org',
  packages = ['eml_parser'],
  use_2to3 = True,
)
